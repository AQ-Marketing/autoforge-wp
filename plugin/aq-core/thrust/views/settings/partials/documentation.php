<?php
/**
 * Documentation block template.
 *
 * Boost edition: intentionally empty. Boost ships no external documentation
 * link (the upstream block pointed at wp-rocket.me). Kept as a no-op so the
 * `render_part( 'documentation' )` calls in the sidebar and dashboard remain
 * valid without surfacing any third-party reference.
 *
 * @since 3.0
 */

defined( 'ABSPATH' ) || exit;
